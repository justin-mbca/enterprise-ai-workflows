enterprise_ai_duckdb:
  target: dev
  outputs:
    dev:
      type: duckdb
      # Use a path relative to the dbt project directory to avoid duplication
      path: warehouse/data.duckdb
      threads: 4
      schema: main
